[project]
name = "inkscape-copilot"
version = "0.1.0"
description = "Minimal prompt-to-action copilot prototype for Inkscape."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["inkscape_copilot"]

